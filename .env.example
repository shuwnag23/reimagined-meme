# Required for server mode. Use a long, unique value.
ADMIN_PASSWORD=replace-with-a-strong-password

# Optional settings
ADMIN_USER=admin
ADMIN_SESSION_SECRET=replace-with-a-long-random-secret
PORT=3000

# Optional persistent-storage locations (relative or absolute paths)
# PICKUP_DATA_DIR=./data
# PICKUP_UPLOAD_DIR=./uploads
